{{ proposed_by.username }} proposed a book swap:
                
Your books:
{% for listing in requested_listings %}- {{ listing.book.title }} by {{ listing.book.authors.all|join:", " }}
{% endfor %}
{{ proposed_by.username }}'s books:
{% for listing in offered_listings %}- {{ listing.book.title }} by {{ listing.book.authors.all|join:", " }}
{% endfor %}
Accept or decline the swap here:
{{ swap_url }}
