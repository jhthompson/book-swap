[project]
name = "book-swap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2",
    "django-isbn-field>=0.5.3",
]

[dependency-groups]
dev = [
    "djade>=1.4.0",
    "ruff>=0.11.7",
]
